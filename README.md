# code-alpha-tasks
A data science project that analyzes the Iris dataset and builds a machine learning classification model using K-Nearest Neighbors (KNN) to predict flower species based on sepal and petal measurements.
